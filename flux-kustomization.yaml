apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: anubis-api-dev
  namespace: flux-system
spec:
  interval: 1m
  path: ./apps/anubis-api/environments/dev
  
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: default

